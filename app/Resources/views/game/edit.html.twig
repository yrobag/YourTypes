{% extends 'base.html.twig' %}

{% block body %}
    <h3>Game edit</h3>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form, { 'attr': {'style':  'color: black;'}}) }}
        <input style="color: black" type="submit" value="Edit" />
    {{ form_end(edit_form) }}


        <a class="toEdit" href="{{ path('app_game_shownext') }}">Back to the next games</a><br>
        <a class="toEdit" href="{{ path('app_game_showprevious') }}">Back to the previous games</a>

            {{ form_start(delete_form) }}
                <input class="toDelete" type="submit" value="Delete">
            {{ form_end(delete_form) }}

{% endblock %}
